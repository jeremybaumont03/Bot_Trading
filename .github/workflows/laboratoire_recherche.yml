name: 🧠 1. Intelligence & Risk

on:
  schedule:
    - cron: '00 21 * * 1-5' # 21:00 UTC
  workflow_dispatch:

concurrency:
  group: usine-trading
  cancel-in-progress: false

jobs:
  run-brain:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      - run: |
          pip install --upgrade pip
          pip install yfinance pandas numpy scikit-learn requests joblib feedparser vaderSentiment statsmodels hmmlearn

      - name: 🔬 Run Labs (Data)
        run: |
          python regime_kmeans.py || true
          python regime_hmm.py || true
          python labo_sentiment.py || true

      - name: 🧠 Run Meta-Controller (Boss)
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python meta_controlleur.py || true

      - name: 💾 Save Intelligence State
        run: |
          git config --global user.name "Hedge Fund Brain"
          git config --global user.email "brain@trading.local"
          git stash || true
          git pull --rebase origin main || true
          git stash pop || true
          git add *_log.json global_settings.json || true
          if ! git diff --staged --quiet; then
            git commit -m "🧠 Market Intelligence Updated - $(date '+%d/%m/%Y %H:%M')"
            git push
          fi
